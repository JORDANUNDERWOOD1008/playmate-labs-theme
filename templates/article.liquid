{% comment %} PLAYMATE LABS — Article Template (Journal Post) {% endcomment %}
{{ 'journal.css' | asset_url | stylesheet_tag }}

<article class="article-page" itemscope itemtype="https://schema.org/Article">

  <!-- ARTICLE HERO -->
  <section class="article-hero">
    <div class="container">
      <div class="breadcrumb">
        <a href="{{ routes.root_url }}">Home</a>
        <span class="breadcrumb-sep">›</span>
        <a href="{{ blog.url }}">{{ blog.title | default: 'Journal' }}</a>
        <span class="breadcrumb-sep">›</span>
        <span>{{ article.title | truncate: 40 }}</span>
      </div>

      <div class="article-hero-meta">
        {%- for tag in article.tags -%}
          <span class="article-tag">{{ tag }}</span>
        {%- endfor -%}
      </div>

      <h1 class="article-hero-title" itemprop="headline">{{ article.title }}</h1>
      <p class="article-hero-excerpt">{{ article.excerpt_or_content | strip_html | truncate: 200 }}</p>

      <div class="article-meta-row">
        <div class="article-author-avatar">
          <span>{{ article.author | slice: 0 }}</span>
        </div>
        <div class="article-meta-details">
          <span itemprop="author" itemscope itemtype="https://schema.org/Person">
            <a href="/pages/about" itemprop="url" style="color:inherit;text-decoration:none;">
              <span itemprop="name">{{ article.author }}</span>
            </a>
          </span>
          <span class="article-meta-sep">·</span>
          <time itemprop="datePublished" datetime="{{ article.published_at | date: '%Y-%m-%d' }}">
            {{ article.published_at | date: '%B %d, %Y' }}
          </time>
          {%- if article.updated_at != article.published_at -%}
            <span class="article-meta-sep">·</span>
            <time itemprop="dateModified" datetime="{{ article.updated_at | date: '%Y-%m-%d' }}">
              Updated {{ article.updated_at | date: '%B %d, %Y' }}
            </time>
          {%- endif -%}
          <span class="article-meta-sep">·</span>
          <span>{{ article.content | strip_html | split: ' ' | size | divided_by: 200 | plus: 1 }} min read</span>
        </div>
      </div>
    </div>

    {%- if article.image -%}
      <div class="article-hero-img">
        <img
          src="{{ article.image | image_url: width: 1400 }}"
          alt="{{ article.image.alt | escape }}"
          itemprop="image"
          loading="eager"
        >
      </div>
    {%- endif -%}
  </section>

  <!-- ARTICLE CONTENT -->
  <section class="article-content-section">
    <div class="container">
      <div class="article-layout">

        <!-- Main Content -->
        <div class="article-body" itemprop="articleBody">
          {%- comment -%} Downgrade any H1 tags in article body to H2 to avoid duplicate H1s {%- endcomment -%}
          {{ article.content | replace: '<h1', '<h2' | replace: '</h1>', '</h2>' }}
        </div>

        <!-- Sidebar -->
        <aside class="article-sidebar">

          <!-- Author Card (E-E-A-T Enhanced) -->
          <div class="article-sidebar-card">
            <h4 class="article-sidebar-heading">About the Author</h4>
            <div class="article-author-card">
              <div class="article-author-big-avatar">
                <span>{{ article.author | slice: 0 }}</span>
              </div>
              <div>
                <p class="article-author-name">
                  <a href="/pages/about" style="color:inherit;text-decoration:none;">{{ article.author }}</a>
                </p>
                {%- if article.author == 'Jordan Underwood' -%}
                  <p class="article-author-role">Founder, Playmate Labs</p>
                {%- else -%}
                  <p class="article-author-role">Playmate Labs Editorial</p>
                {%- endif -%}
              </div>
            </div>
            {%- if article.author == 'Jordan Underwood' -%}
              <p style="font-size:0.8125rem;line-height:1.5;margin-top:0.75rem;opacity:0.85;">
                Jordan founded Playmate Labs to bring science-backed aphrodisiac chocolates to UK couples. He works directly with artisan chocolatiers and ingredient researchers to develop every product.
              </p>
              <a href="/pages/about" style="font-size:0.8125rem;color:var(--gold,#c9a96e);text-decoration:none;display:inline-block;margin-top:0.5rem;">
                Read full bio →
              </a>
            {%- endif -%}
          </div>

          <!-- Tags -->
          {%- if article.tags.size > 0 -%}
          <div class="article-sidebar-card">
            <h4 class="article-sidebar-heading">Filed under</h4>
            <div class="article-tag-list">
              {%- for tag in article.tags -%}
                <a href="{{ blog.url }}/tagged/{{ tag | handleize }}" class="article-tag">{{ tag }}</a>
              {%- endfor -%}
            </div>
          </div>
          {%- endif -%}

          <!-- Share -->
          <div class="article-sidebar-card">
            <h4 class="article-sidebar-heading">Share this story</h4>
            <div class="article-share-btns">
              <a href="https://www.instagram.com/" target="_blank" rel="noopener" class="article-share-btn instagram">
                <i class="fab fa-instagram"></i> Instagram
              </a>
              <a href="https://twitter.com/intent/tweet?text={{ article.title | url_encode }}&url={{ shop.url }}{{ article.url | url_encode }}" target="_blank" rel="noopener" class="article-share-btn twitter">
                <i class="fab fa-twitter"></i> Twitter
              </a>
            </div>
          </div>

          <!-- Newsletter Sidebar -->
          <div class="article-sidebar-newsletter">
            <span class="section-eyebrow" style="color:var(--gold);font-size:0.6875rem;">Stay in the loop</span>
            <h4>New drops &amp; rituals to your inbox</h4>
            <form action="/contact#contact_form" method="post" class="article-newsletter-form">
              <input type="hidden" name="form_type" value="customer">
              <input type="hidden" name="utf8" value="✓">
              <input type="hidden" name="contact[tags]" value="newsletter,journal-reader">
              <input type="email" name="contact[email]" placeholder="your@email.com" required class="form-input form-input-sm">
              <button type="submit" class="btn btn-primary btn-sm" style="width:100%;justify-content:center;margin-top:0.5rem;">Subscribe</button>
            </form>
          </div>

        </aside>
      </div>
    </div>
  </section>

  <!-- A/B TEST: PRODUCT CTA CARD (Variant B only — hidden by default, shown via JS) -->
  <section class="article-cta-card-section" id="ab-cta-card" style="display:none;" data-test="blog-cta-v1">
    <div class="container">
      <div class="article-cta-card">
        <div class="article-cta-card-img">
          <img
            src="https://cdn.shopify.com/s/files/1/0637/2176/7068/files/newbright1.png?v=1762956658&width=400"
            alt="Aphrodisiac Hot Choc Spoons by Playmate Labs"
            loading="lazy"
            width="400"
            height="533"
          >
        </div>
        <div class="article-cta-card-content">
          <span class="article-cta-card-eyebrow">Enjoyed this article?</span>
          <h3 class="article-cta-card-title">Aphrodisiac Hot Choc Spoons</h3>
          <p class="article-cta-card-desc">Stir real dark chocolate and aphrodisiac botanicals into warm milk. Three flavours. Science-backed ingredients. The perfect excuse to slow down together.</p>
          <div class="article-cta-card-price">
            <span class="article-cta-card-price-current">From £14.99</span>
            <span class="article-cta-card-price-compare">£29.98</span>
          </div>
          <a href="/products/aphrodisiac-hot-choc-spoons" class="btn btn-primary article-cta-card-btn" id="ab-cta-btn">
            Shop Hot Choc Spoons
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- RELATED ARTICLES -->
  {%- if blog.articles.size > 1 -%}
  <section class="article-related section bg-ivory">
    <div class="container">
      <div class="section-header">
        <span class="section-eyebrow">Keep Reading</span>
        <h2>More from the Journal</h2>
      </div>
      <div class="article-related-grid">
        {%- assign count = 0 -%}
        {%- for a in blog.articles -%}
          {%- if a.id != article.id and count < 3 -%}
            <a href="{{ a.url }}" class="journal-card reveal">
              <div class="journal-card-img">
                {%- if a.image -%}
                  <img src="{{ a.image | image_url: width: 600 }}" alt="{{ a.image.alt | escape }}" loading="lazy">
                {%- else -%}
                  <div class="img-placeholder"><span>📖</span></div>
                {%- endif -%}
              </div>
              <div class="journal-card-body">
                <p class="journal-card-category">{{ a.tags.first | default: 'Journal' }}</p>
                <h3 class="journal-card-title">{{ a.title }}</h3>
                <div class="journal-card-meta">
                  <span>{{ a.published_at | date: '%b %d, %Y' }}</span>
                </div>
              </div>
            </a>
            {%- assign count = count | plus: 1 -%}
          {%- endif -%}
        {%- endfor -%}
      </div>
      <div style="text-align:center;margin-top:2.5rem;">
        <a href="{{ blog.url }}" class="btn btn-secondary">View All Articles</a>
      </div>
    </div>
  </section>
  {%- endif -%}

</article>

<!-- A/B TEST: Blog CTA Card v1 — variant assignment + GA4 tracking -->
<script>
(function(){
  var TEST_NAME = 'blog_cta_v1';
  var COOKIE_NAME = 'ab_' + TEST_NAME;
  var EXPIRY_DAYS = 42;

  function getCookie(name) {
    var match = document.cookie.match(new RegExp('(^| )' + name + '=([^;]+)'));
    return match ? match[2] : null;
  }

  function setCookie(name, value, days) {
    var d = new Date();
    d.setTime(d.getTime() + days * 86400000);
    document.cookie = name + '=' + value + ';expires=' + d.toUTCString() + ';path=/;SameSite=Lax';
  }

  function gaEvent(name, params) {
    if (typeof gtag === 'function') {
      gtag('event', name, params);
    }
  }

  // Assign or retrieve variant
  var variant = getCookie(COOKIE_NAME);
  if (!variant) {
    variant = Math.random() < 0.5 ? 'control' : 'cta_card';
    setCookie(COOKIE_NAME, variant, EXPIRY_DAYS);
  }

  // Fire impression event
  gaEvent('ab_test_view', {
    test_name: TEST_NAME,
    variant: variant,
    page_path: window.location.pathname
  });

  // Show CTA card for variant B
  if (variant === 'cta_card') {
    var card = document.getElementById('ab-cta-card');
    if (card) card.style.display = '';

    // Track CTA click
    var btn = document.getElementById('ab-cta-btn');
    if (btn) {
      btn.addEventListener('click', function() {
        gaEvent('ab_cta_click', {
          test_name: TEST_NAME,
          variant: variant,
          product: 'aphrodisiac-hot-choc-spoons',
          page_path: window.location.pathname
        });
      });
    }
  }
})();
</script>
